{
  "name": "tulip",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run build:native && npm run build:asmjs",
    "autogypi": "autogypi",
    "node-gyp": "node-gyp",
    "emcc-path": "emcc-path",
    "copyasm": "copyasm",
    "build:native": "autogypi -r build && node-gyp -C build/native configure build && copyasm build/native dist",
    "build:asmjs":  "autogypi -r build && node-gyp -C build/asmjs  configure build --asmjs=1 && copyasm build/asmjs dist",
    "build:tulip": "cd tulip && make tiamalgamation",
    "install": "make --directory ./tulip tiamalgamation"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "autogypi": "^0.2.2",
    "nbind": "^0.3.14",
    "node-gyp": "^3.6.2"
  }
}
